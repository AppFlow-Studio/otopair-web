'use client'

// Merged into /director — Stripe Connect health lives under the Stripe tab.
import { ShopsRedirect } from '../_redirect'

export default function Page() {
  return <ShopsRedirect to="/director#stripe" />
}
