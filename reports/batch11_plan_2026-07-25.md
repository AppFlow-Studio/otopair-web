# Batch 11 Plan — validate round-8 fixes (written 2026-07-25)

**Round 8 shipped:** commit `62648f3`, deployed to `dev:third-bird-914`. Fixes derived from the batch-10 audit (`convex/vehicleEnrichment/BATCH10_VALIDATION.md`). Ground truths + per-vehicle verdicts preserved in `reports/ground-truth-batch10/` — batch 11 re-uses them; no new ground-truth research needed for the re-run VINs.

## Vehicle set

Re-runs (purge + re-enrich — destructive, spends credits, ~$1.20 + 15 min each):

| VIN | Vehicle | Fixes it exercises |
|---|---|---|
| 1G1AT58H897221703 | 2009 Cobalt | EPS suppression; speeds-vs-unit reconcile; Cruze-cluster fitment refutes (oil filter/o-ring/coil); DEX-COOL interval floor; commodity-source gate (drain plug gasket) |
| 1FTPW14556FB15661 | 2006 F-150 | Same-platform fitment refutes (belt/thermostat/gasket/rear rotor); MERCON V/LV verifier trap; trim guard (Lariat); coolant flat-flag ceiling; diff parts kept (has diff) |
| 3GYFNBE34ES609578 | 2014 SRX | FFV flag; wrong-generation belt refute; FWD diff-part rejection (gear oil); estimated-status rows |
| 5FRYD4H2XEB028867 | 2014 MDX | Oil-viscosity-grade refute (5W-20 part on 0W-20); EPS list (deterministic now); ATF interval band unchanged-check |

Optional 5th (fresh VIN, regression + EPS/trim coverage on an untouched nameplate): any 2009–2013 Toyota Corolla or 2006–2011 Honda Civic VIN — exercises the known-EPS list on a platform batch 10 never saw, plus the trim guard on a high-volume decode. BMW 745Li re-run is optional (validates brake-fluid months floor + 745Li trim guard) — lower priority since its only P1 was the interval.

## Commands (from repo root, targets dev:third-bird-914)

```
npx convex env list                     # ANTHROPIC_API_KEY credit check
npx convex run vehicleEnrichment/runPublic:purgeAndRerun '{"vin":"<VIN>"}'   # per re-run VIN
npx convex run vehicleEnrichment/runPublic:b8collect '{"vin":"<VIN>"}'       # after completion
```
Poll with `diagnoseVin:enrichmentLockState`; the `go`/`purgeAndRerun` CLI printing bare "Error" after a long poll is the known action-timeout artifact, not failure.

## Pass criteria (defect → expected batch-11 outcome)

1. **Cobalt oil filter/o-ring/coil**: PF2257G, 55593191, 25186687 absent (refuted or never extracted). Oil filter ideally PF457G; honest null acceptable. o-ring on a spin-on engine must NOT exist.
2. **Cobalt PS**: `power_steering_type=electric`, no ps fluid capacity/part/intervals, NO Power Steering Flush interval row.
3. **Cobalt transmission**: speeds ≠ 5-with-automatic. Either speeds 4 (reconciled, `trans_speeds_reconciled:4T45` error present) or nulled/flagged. DEXRON-VI retained.
4. **Cobalt coolant interval**: no 30k/24mo on DEX-COOL (≥50k mi / ≥36 mo, or honest null). Watch adversarial `coolant_flush` suspect flag.
5. **Cobalt drain plug gasket**: absent unless it carries a source_domain (`commodity_no_source` rejection in logs otherwise).
6. **F-150 parts**: rear rotor ≠ 5L3Z-1125-BA (front part); thermostat ≠ RT-1194; belt ≠ 5L3Z-8620-BA (4.2L); intake gasket ≠ 4L3Z-9461-AA (4.6L). Correct values or honest nulls both pass — no confidently-wrong survivors.
7. **F-150 trans fluid**: MERCON V stored with NO `trans_fluid_suspect:*expected=*LV` error (prompt trap validated).
8. **F-150 trim**: `Lariat` (config_key `..._lariat_...`), decode log shows normalizer-trim rejection if the LLM tries FX4 again.
9. **F-150 coolant**: 20.9 qt with at most ONE informational band flag (no flat 4-22 flag).
10. **SRX fuel**: E85 claim either absent or flagged `flex_fuel_claim_uncorroborated` with confidence ≤0.6.
11. **SRX belt**: ≠ 12677093 (2017+ Colorado); 12636139 or honest null.
12. **SRX gear oil**: no gear_oil/friction_modifier fitment (`no_differential` rejection in logs).
13. **All configs**: fallback interval rows carry `status:"estimated"` (never "scheduled"); Deep Dive shows the `est.` badge; sources render on evidence/parts/intervals panels.
14. **MDX oil part**: 08798-9032 (5W-20) refuted/absent; 0W-20-grade part or honest null.
15. **No regressions**: trans fluids still 5/5; batch-10 PASS fields unchanged (spot-check engine codes, oil specs, plugs per ground-truth files); MDX cabin filter SDA-407 and Cobalt air filter 25894265 KEPT (verified-correct supersessions — a refute of either is a new false-positive bug).

## Reporting

`convex/vehicleEnrichment/BATCH11_VALIDATION.md` per the established convention; append per-fix ✅/❌ against the criteria above; update memory (`enrichment-batch11-results`).
